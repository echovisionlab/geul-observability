apiVersion: 1

groups:
  - orgId: 1
    name: durable-audit
    folder: Geul Operations
    interval: 1m
    rules:
      - uid: geul-audit-append-failed
        title: Durable audit append failed
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: loki
            model:
              datasource:
                type: loki
                uid: loki
              editorMode: code
              expr: 'sum(count_over_time({service_namespace="geul", deployment_environment="production", exporter="otlp", retention_class="application"} | json event="body.event" | event="audit.append.failed" [1m]))'
              intervalMs: 1000
              maxDataPoints: 43200
              queryType: instant
              refId: A
          - refId: B
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [B]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 0s
        annotations:
          summary: A Domain Audit or Security Access record was not persisted
          description: Inspect the exact action, reason, request ID, and trace ID in Service Overview and the owning service logs.
          runbook_url: /d/geul-service-overview/service-overview
        labels:
          domain: audit
          notification_stage: disabled
          severity: critical
